{{ "prosmash-hp-categories.css" | asset_url | stylesheet_tag }}
{{ "prosmash-global.css" | asset_url | stylesheet_tag }}

<section class="categories">
  <div class="mdp-container">
    <div class="heading-container">
      <h3>Categories</h3>
    </div>
    <div class="mdp-col mdp-row-full categories-grid">
      {% for block in section.blocks %}
        {% assign col = block.settings.collection %}
        {% if col != blank %}
          <div class="category-item">
            <a href="{{ col.url }}">
              {% if col.featured_image %}
                <div class="img-container">
                  <div class="canvas" style="
                      background-image: url('{{ block.settings.collection_img | image_url: width: 205 }}');
                      background-color: #F0F0F0;
                      background-size: cover;
                      background-position: center;
                      width: 205px;
                      height: 205px;">
                  </div>

                </div>
              {% endif %}
              <div class="item-title">{{ col.title }}</div>
            </a>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>



{% schema %}
{
  "name": "Prosmash HP Categories",
  "settings": [],
  "blocks": [
    {
      "type": "collection",
      "name": "Collection",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Select Collection"
        },
        {
          "type": "image_picker",
          "id": "collection_img",
          "label": "Collection Image"
        },
      ]
    }
  ],
  "presets": [
    {
      "name": "Prosmash HP Categories",
      "category": "Prosmash"
    }
  ]
}
{% endschema %}
